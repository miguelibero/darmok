#pragma once

namespace darmok
{
    enum class ModelDataFormat
    {
        Binary,
        Json,
        Xml
    };

    enum class ModelMaterialBlendMode
    {
        Default,
        Additive
    };
}