#pragma once

namespace darmok
{
    enum class ModelDataFormat
    {
        Binary,
        Json,
        Xml
    };

    enum class ModelMaterialOpacity
    {
        Opaque,
        Cutout,
        Transparent
    };
}